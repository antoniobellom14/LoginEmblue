import React from 'react';

const Title = ({ text }) => {

    return (
        <div className='title'>
            <label>
                {text}
            </label>
        </div>
    )
};

export default Title